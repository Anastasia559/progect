

    <!--/.Navbar -->
    <div class="main" style="margin-top: -40px;">
        <div class="content">
            <div class="title mt-5">
                <p class="secondary-text tn-text "> <a href="/" class="yellow_text underline">Главная</a>
                    &nbsp;/&nbsp;<a href="/soomo" class="yellow_text underline">ЦООМО</a>
                    &nbsp;/&nbsp;<a href="/restoresertfrommesagesoomo" class="yellow_text underline">Подтверждение данных сертификата после
                        военной службы </a>
                    &nbsp;/&nbsp; <span class="secondary-text">Подтверждение данных</span>
                </p>
                <h2 class="white-text  title_text mt-5">Подтверждение данных</h2>

            </div>

        </div>

    </div>

  
    <div class="modal fade" tabindex="-1" role="dialog" id="modalOk">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-body">
                    <p>Успехов! </p>
                </div>
                <div class="modal-footer">
                    <a href="/personalcabinet/#enrollee" class="btn   btn-default bg-yellow btn-sm">Войти</a>
                    <button type="button" class="btn btn-danger btn-sm" data-dismiss="modal">Закрыть</button>
                </div>
            </div><!-- /.modal-content -->
        </div><!-- /.modal-dialog -->
    </div><!-- /.modal -->


    <div id="content" class="content">
        <div class="bg-white shadow border-r8 p-4  mt-2">
                <p class="font-20 color-blue font-weight-bold">Данные сообщения абитуриента</p>

            <div class="form-group animated fadeIn" id="search_form">
                <div class="form-row">
                    <div class="form-group col-md-6 m-0">
                        <label>ФИО</label>

                        <label id="fio">{{ab_fio}}</label>
                    </div>
                    <div class="form-group col-md-6 m-0">
                        <label> Школа</label>
                        <label id="school">{{school}}</label>
                    </div>
                </div>
                <div class="form-row">
                    <div class="form-group col-md-6 m-0">
                        <label>№ сертификата</label>
                        <label id="numberSert">{{number}}</label>
                    </div>
                    <div class="form-group col-md-6 m-0">
                        <label>Номер телефона</label>
                        <label id="phone">{{abit_phone}}</label>
                    </div>
                </div>

                <div class="form-row">
                    <div class="form-group col-md-6 m-0">
                        <label>Область</label>
                        <label id="region">{{region }}</label>
                    </div>

                    <div class="form-group col-md-6 m-0">
                        <label> Район</label>
                        <label id="district">{{district}} </label>
                    </div>
                </div>
                <div class="form-row">
                    <div class="form-group col-md-12 m-0">
                        <label> Сообщение</label>
                        <label id="message">{{message}} </label>
                    </div>
                </div>

            </div>
            <div class="row">
                <div id="ort_div" class="col">
                    <p>ОРТ <span type="button" id="turn" style="z-index:1000;">Поворот<span></p>
                    <img id="ort_image" draggable="true" class="img-fluid zoom   animated fadeIn" src="data:img/png;base64,{{ort}}" alt="ORT">
                </div>
                <div class="col" id="pas_div">
                    <p>Паспорт</p>
                    <img id="pas_image" class="img-fluid zoom   animated fadeIn" src="data:img/png;base64,{{pas}}" alt="Passport">
                </div>
                <div class="col" id="pas2_div">
                    <p>Паспорт 2</p>
                    <img id="pas2_image" class="img-fluid zoom   animated fadeIn" src="data:img/png;base64,{{pas2}}" alt="Passport2">
                </div>
                <div class="col" id="ate_div" style="display:none">
                    <p>Аттестат</p>
                    <img id="ate_image" class="img-fluid zoom   animated fadeIn" src="data:img/png;base64,{{ate}}" alt="Atestat">
                </div>

                <div class="col" id="mil_div">
                    <p>Военный билет</p>
                    <img id="mil_image" class="img-fluid zoom   animated fadeIn" src="data:img/png;base64,{{mil}}" alt="Military">
                </div>

            </div>
        </div>


        <div class="row mx-md-3">
            <div class="col-md-6">
                <div class="bg-white shadow border-r8 p-4  mt-3">
                    <p class="font-20 color-blue font-weight-bold">Данные по базе</p>
       <p><span class="font-20  font-weight-bold">Область</span> {{region}}</p>
       <p><span class="font-20  font-weight-bold">Район</span> {{district}}</p>
       <p><span class="font-20  font-weight-bold">Учебное заведение</span> {{school}}</p>
       <p><span class="font-20  font-weight-bold">Телефон</span> {{abit_phone}}</p>
                    


                </div>


            </div>
            <div class="col-md-6">
                <div class="bg-white shadow border-r8 p-4  mt-3">
                    <p class="font-20 color-blue font-weight-bold">Корректировка базы</p>
                    <div class="  ">
                        <div class="form-group p-2 border  border-r8">
                            <label class="textarea display-5 sm-text color-blue font-weight-normal"
                                for="region">Выберите область</label>
                            <select class="usersert form-control  text-center mx-auto border-none" id="region"
                                onchange=getRegions('region','district')>
                                <option value="0">1111</option>
                               
                            </select>
                        </div>
                        <div class="form-group  p-2 border  border-r8">
                            <label class="textarea display-5 sm-text color-blue font-weight-normal"
                                for="district">Выберите район</label>
                            <select class="usersert border-none form-control  text-center mx-auto" id="district"
                                onchange=getRegions('district','school')>
                                <option value="0"></option>
                            </select>
                        </div>
                        <div class="form-group p-2 border  border-r8">
                            <label class="textarea display-5 sm-text  color-blue font-weight-normal"
                                for="school">Выберите учебное заведение</label>
                            <select class="usersert form-control border-none text-center mx-auto" id="school"
                                onchange=getRegions('school','usersert')>
                                <option value="0"></option>
                            </select>
                        </div>
                        <div class="form-group p-2 border  border-r8">
                            <label class="textarea display-5 sm-text  color-blue font-weight-normal"
                                for="school">Введите номер телефона</label>
                            <input class="usersert form-control border-none text-center mx-auto" id="phone"
                                 value="{{abit_phone}}" oninput="if(value.length>10)value=value.slice(0,10)"> 
                            </select>
                        </div>
                        <div class="form-group p-2">
                            <button type="submit" class="checksertbutton bg-yellow btn btn-primary btn-block"
                                id="checksertbutton" disabled>Изменить</button>

                        </div>
                    </div>
                </div>


            </div>

        </div>



    </div>

  
     <script>

$('#turn').on('click', function(){
    var angle = ($('#ort_image').data('angle') + 90) || 90;
    $('#ort_image').css({'transform': 'rotate(' + angle + 'deg)'});
    $('#ort_image').data('angle', angle);
});

   
   


{{!-- Filter --}}

  window.addEventListener("load", function(event) {
    getRegions('fio','region');
  });


  
document.getElementById('school').addEventListener("click", function(){document.getElementById('usersert').disabled = false;},);
function addOption(elem, arrFromServer){
    var select = document.getElementById(elem);
        switch(elem) {
                  case 'region':  
                        for(i = 0; i < arrFromServer.length; i = i + 1) {
                            select.options[select.options.length] = new Option(arrFromServer[i].region, arrFromServer[i].id_region);
                        };
                    break;
                  case 'district':   
                        for(i = 0; i < arrFromServer.length; i = i + 1) {
                            select.options[select.options.length] = new Option(arrFromServer[i].district, arrFromServer[i].id_district);
                        };
                        district.disabled = false;
                    break;
                  case 'school':   
                        for(i = 0; i < arrFromServer.length; i = i + 1) {
                            select.options[select.options.length] = new Option(arrFromServer[i].school, arrFromServer[i].id_school);
                        };
                        school.disabled = false;
                    break;
        }
}

function removeAllOptions(elem){
    var select = document.getElementById(elem);
    console.log(select);
    select.options.length = 1;
}

function getRegions(fromelem, toelem) {
    var abfio = '';
    var selid = '';
    var nameschool = '';
    var abphone = '';
    var message = '';
    var idregion = document.getElementById('region').value;
    var iddistrict = document.getElementById('district').value;
    switch(toelem) {
            case 'region':
                // removeAllOptions('region');
                // removeAllOptions('district');
                // removeAllOptions('school');
                district.disabled = true; 
                school.disabled = true; 
               
                selid = 0;
                break;
            case 'district':  
                removeAllOptions('district');
                removeAllOptions('school');
                school.disabled = true; 
               
                selid = document.getElementById(fromelem).value;
                break;
            case 'school':
                removeAllOptions('school');
                school.disabled = true;
                
                selid = document.getElementById(fromelem).value;
                break;
    }
    var xmlhttp = new XMLHttpRequest();
    var arrFromServer;
    var body = 'elem=' + toelem + '&selid=' + selid + '&idregion=' + idregion + '&iddistrict=' + iddistrict;
    console.log(body);
    xmlhttp.onreadystatechange=function(){
        if(xmlhttp.readyState === XMLHttpRequest.DONE && xmlhttp.status === 200) {
        arrFromServer = JSON.parse(xmlhttp.responseText);
        console.log(arrFromServer);
        addOption(toelem, arrFromServer);
        };
    }; 
    xmlhttp.open('post', '/getelements', true);
    xmlhttp.setRequestHeader('Content-Type', 'application/x-www-form-urlencoded', 'charset=utf-8');
    xmlhttp.send(body);
}


</script>